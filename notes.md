# finance_agent 笔记

---

# Day 1 笔记 · 骨架搬迁 + 数据层

## 做出来了什么

从 forge 搬来骨架，把数据层立起来。

```
fa/
├── events.py        ← forge 原样
├── agent.py         ← forge 搬（Session 循环 + 事件回调）
├── permissions.py   ← forge 原样
├── skills.py        ← forge 搬（三级渐进披露）
├── prompt.py        形状搬，内容换成财务的
├── main.py          形状搬，命令换
├── config.py        13 个固定类目 + 路径 + 模型工厂
├── models.py        Transaction + 金额
├── ingest.py        CSV → list[Transaction]
└── tools/           _util / skills / __init__

data/generate.py     合成账单生成器（埋 6 个坑）
```

验收：`pytest` 75 passed；`python -m data.generate --check` 确认 6 个坑都在；
REPL 端到端能对话。

## 今天学到的

### 1. 为什么第一天就做「确定性核心」

直觉上会觉得：先把 agent 搭起来，能对话了再慢慢加功能。

但顺序反了。这个项目的**卖点之一是「可度量」**，而度量的前提是有真值。
`query.py` 是确定性的，所以它自己就是标准答案 —— 第八天的问答评测不需要人工
标注 30 道题的答案，直接拿它算就行。

**没有确定性核心，就没有 oracle；没有 oracle，后面所有评测都无从谈起。**
所以它必须先做，而且必须做对。

### 2. 金额为什么用 Decimal 而不是 float

这个项目的承诺是「答案可复现、可核对」。用 float 会把这条承诺自己拆掉：
1100 笔求和下来误差落在 1e-10 量级，肉眼看不出来，但第八天的评测做的是
**精确比对**（agent 通过工具链拿到的数字 vs `query.py` 直算）—— 浮点误差会让
本来正确的答案被判成错的，而且时有时无，最难查的那种。

不过真正的坑不在「float 有误差」这种人人皆知的话上，而在一个更隐蔽的地方：

```python
>>> from decimal import Decimal
>>> Decimal(0.1)
Decimal('0.1000000000000000055511151231257827021181583404541015625')
>>> Decimal("0.1")
Decimal('0.1')
```

**`Decimal(0.1)` 是脏的，而且不是 Decimal 的错。** `0.1` 这个字面量在 Python 里
求值时就已经是个二进制近似值了，Decimal 只是诚实地把收到的数原样存下。
所以 `money()` 必须走字符串：

```python
def money(value) -> Money:
    return Decimal(str(value)).quantize(CENT, rounding=ROUND_HALF_UP)
```

外部输入（CSV、模型返回、用户输入）**必须**经过它。这不是洁癖 —— 那是金额
进系统的唯一一道闸。

（顺带：`ROUND_HALF_UP` 是明确的。默认的取整模式是银行家舍入
`ROUND_HALF_EVEN`，`2.5 → 2`、`3.5 → 4`，和人对账时的直觉不一样，
结算场景里选哪个是业务决定，不该靠默认值。）

### 3. 金额符号约定必须写进 README

**支出为正，收入和退款为负。** 银行流水一般是反过来的。

选「支出为正」是因为「上个月花了多少」直接 `sum()` 才是直觉的；反过来则每个
查询都得先取负号，迟早有一处漏了 —— 而且漏了之后数字看着还挺合理，不会有人
发现。

关键在于**它反直觉**，所以必须写进 README。凡是「不看文档一定会猜错」的约定，
都不能只存在于代码里。`ingest.py` 里的 `flip_sign` 旋钮就是为接真实数据准备的。

### 4. 「确保坑在数据里」要用反查，不能用断言

6 个坑是后面所有异常检测评测的真值。生成器一开始我就写了断言 —— 但后来发现
断言有个结构性问题：**它跟着代码一起改**。

你在 `generate()` 里写 `assert 坑在数据里`，然后调了生成逻辑让坑没了 ——
你会顺手把断言也改掉（因为测试红了很烦），于是坑没了且无人知道。

所以 `--check` 是**从产物里反查**：只看交易和标签，不看生成逻辑、不看 seed、
不引用上面的常量。它验证的是**数据**，不是**生成代码的意图**。

这个区别今天当场就兑现了 —— 见坑 1。

## 今天踩的坑

### 坑 1：坑 6 是靠随机采样碰运气的（真问题）

坑 6 是「分类歧义商户」：同一个 `AMZN Mktp US*2H4KJ`，金额从几块到几百块都有，
小额的像订阅、大额的像购物，纯看商户名判不出来。

第一版靠随机金额去碰这个跨度，`--check` 里也过了（12×）。然后我把每月的购物
频次从 5–8 调到 7–10 想让总数更接近 1100 —— **RNG 流跟着变了，跨度掉到 6.7×，
坑没了**。

`--check` 当场报出来：`GONE 6 分类歧义 —— 最大/最小 = 6.7×`。

教训：**一个「已知真值」如果不写死，它就不是已知的。** 六个坑现在全部显式注入，
包括坑 6 的那两个端点（4.99 和 329.00），跨度稳定在 65.9×。

顺带这也证明了反查的价值：如果是生成时的断言，我改完频次会顺手把断言改了，
然后坑没了而没人知道 —— 异常检测的评测会「全过」，那比失败更糟。

### 坑 2：`money()` 抛的异常没人接住（真 bug）

`ingest.py` 里我写了：

```python
try:
    amount = money(raw[mapping["amount"]])   # 遇到「一百块」会抛 InvalidOperation
except (KeyError, AttributeError) as exc:    # ← 没接住它
    raise IngestError(...)
```

`decimal.InvalidOperation` 是 `ArithmeticError` 的子类，两个 except 都不匹配，
于是原始异常直接冒出去，消息是：

```
decimal.InvalidOperation: [<class 'decimal.ConversionSyntax'>]
```

对用户毫无用处 —— 他不知道是哪一行、哪个值。修法是加一个 `parse_amount()`，
和 `parse_date()` 对称，在那一层把异常转成带原始值的说法。

**教训**：写 `except` 的时候，要问的不是「可能抛什么」，而是「**报错信息对用户
有用吗**」。类型对不对只是第一层。

### 坑 3：我的浮点断言写错了

想证明「Decimal 精确、float 不准」，写了：

```python
assert sum([0.10] * 10) != 1.00   # ← 这条其实是 1.0 == 1.0，断言失败
```

`0.1` 累加 10 次**恰好**是 1.0 —— 误差在这个例子里自己抵消了。换成 3 次就露馅：

```python
assert 0.10 + 0.20 != 0.30        # 0.30000000000000004
assert sum([0.10] * 3) != 0.30
```

**教训**：写「反例」断言时，得真的跑一遍确认它失败了。凭印象举的例子，
很可能恰好是那个不失败的。

## 从 forge 搬什么、丢什么

**原样搬**：`events.py`（一字不改）、`permissions.py`、`agent.py` 的 Session
循环 / 事件回调 / 悬空 tool_call 保证 / 指纹门控 / 上下文超限回滚 /
`content_filter` 识别、`skills.py` 的三级渐进披露。

**丢掉 `workspace.py`（路径边界）。** 这是今天最值得说的一处取舍。

forge 里那套边界判定（resolve 之后判前缀、符号链接、`..`）花了不少心思，但那
**是编码 agent 才需要的**：它的模型可以传任意路径进来读文件。财务 agent 的模型
**不接收任何路径** —— 读账单是 MCP server 内部的事（Day 7），读知识库是 ingest
的事。**没有入口，就不需要守门。**

代码留着不用，最大的害处不是占地方，是下一个人会以为它有意义，然后在一个
不需要它的地方维护它。

**换掉内容、保留形状**：`prompt.py`、`main.py`。

搬迁之后补了 75 条测试（含从 forge 带过来改的），主要作用是**证明没搬坏** ——
骨架和领域无关，所以断言基本原样保留。改那些断言之前，先想清楚是不是把一条
真正的设计约束给删了。

---

# Day 2 笔记 · 确定性核心（query + anomalies）

## 做出来了什么

```
fa/query.py      确定性查询引擎（纯函数，项目的心脏）
fa/anomalies.py  四个检测器：重复扣款 / 涨价 / 订阅清单 / 异常大额
```

验收：`pytest` 134 passed；四个检测器在真账单上的检出/误报数字见 README。

## 今天学到的

### 1. oracle 必须返回结构化结果，不能返回字符串

`query()` 返回 `QueryResult`（笔数、数值、分组明细、用到的条件），而不是一句
渲染好的话。理由不是审美：

**一个返回字符串的查询引擎没法当 oracle。** 第八天的问答评测要拿它算出标准
答案，然后和 agent 通过工具链拿到的数字比对。如果它返回「2026-07 月餐饮外卖
合计 1,234.56 元」，评测脚本就得反过来解析这行文本 —— 那是在测试渲染器，
不是在测试查询。而且渲染格式一改（加个千分位、换个词），评测就全崩。

同理，筛选条件也原样带回来（`Filters`）。回答里必须复述条件（prompt 第三条
铁律），而「2026-07-01 ~ 2026-07-31，类目=餐饮外卖，合计」这句话由谁来说都得
一样，所以 `describe()` 放在 query.py 里，而不是各工具各写一遍。

### 2. 「超 p95 算异常」是错的，改成 Tukey 围栏

原方案写的是「类目内金额超 p95」。写的时候没细想，实现时一算：

**p95 按定义就会标出 5% 的交易。** 这份数据 1061 笔，5% 是 53 笔。
一个标出 53 笔的异常检测等于没有检测 —— 用户看两次就会学会无视它，而**那之后
真正的那一笔也一起被无视了**。

改成 Tukey 围栏 `Q3 + k×IQR`（k=3，统计学上的「远外侧」）。它问的是
「离主体有多远」，不是「排在前面几名」。同一个数据集上，它只揪出真正离群的。

这个区别值得记住：**「前 5%」是一个相对位置，「离群」是一个绝对距离。**
用相对位置做异常检测，样本量一变结果就全变 —— 数据从 1000 笔涨到 10000 笔，
你还是会得到 500 笔「异常」。

### 3. 分组一退化，检测器立刻变成噪音机

写完跑了一遍，两种分组的结果：

```
group_by="merchant"  →  1 笔   （BEST BUY 3899，7.08×）
group_by="category"  →  31 笔  （房租被标了 12 次）
```

原因是**那一刻还没有分类**（分类是第四天的事），所有交易挤在同一个「未分类」
组里，围栏退化成全局围栏 —— 房租 3250 相对全体交易当然是异常。

这是第 2 条那个坑从另一扇门进来了：**问题的根不在阈值，在分组。**
异常是相对的，没有有效的组就没有基准。

所以加了一道防呆：`group_by="category"` 而一笔都没分类时直接拒绝，并告诉调用方
换 `merchant`。**与其安静地返回一堆噪音，不如说清楚为什么给不了。**

（顺带：`group_by="none"` 也会给出 31 笔，但那个是**合理的** —— 房租相对全体
交易确实离群。同一个数字，一个有意义一个没意义，区别在于调用方想要什么。）

### 4. 订阅识别要两道闸，不是一道

要认出「周期性扣款」，最直觉的判据是「金额固定」。但只卡这一条会误报：

**一杯 5.00 的咖啡，一年里恰好买到 4 次同样金额，和订阅长得一模一样。**

所以加了第二道闸：**间隔要规律**（间隔的变异系数 ≤ 0.25）。订阅是按月扣的，
随机消费不是。再加一条「间隔至少 20 天」，把每天都去的小店也挡在外面。

`tests/test_anomalies.py::test_same_amount_at_random_intervals_is_not_a_subscription`
就是这条闸存在的全部理由。

反过来说，涨价检测也是靠这道闸才成立的：没有「金额固定」，一家去了 6 次的餐厅
会在「前三次均价 vs 后三次均价」上触发涨价告警 —— 那不是涨价，**那是你最近
吃得贵了**。

### 5. 年化的折算方式：日历月，不是平均天数

「这个订阅一年多少钱」要折算。第一版按「跨度 ÷ 间隔数」算平均月长：

- 1~6 月这个窗口的平均月长是 30.2 天（冬季月份长），而一年平均是 30.44 天
- 于是 10.00 的月订阅被算成 10.08

**每一笔都差 0.8%，而且方向固定** —— 这种误差不会在求和时互相抵消，只会累积。
房租 39000 会算成 39093。

改成按**日历月计数**：`每月次数 = 笔数 ÷ 跨过的日历月数`。对「每月 5 号扣一次」
给出精确的 1，房租年化就是精确的 39000.00。

教训：**能数整数就别去算平均值。** 日历月的个数是确定的，平均月长是估的。

（局限也写进了 docstring：这是**按当前单价折算的未来一年**，不是「过去一年实际
花了多少」。NETFLIX 涨过价，两者不一样。）

### 6. 闭区间，以及为什么它容易写错

`date_from` / `date_to` 都是**含当天**的。用户说「7 月」，7 月 31 号的消费当然
要算进去。

写错成开区间，丢的是**边界那天**的消费。这个错误特别难发现，因为用户不知道
正确答案是多少 —— 他看到的是一个偏小的、看起来很合理的数字。

所以专门有 `test_date_range_is_inclusive_on_both_ends` 钉两端。

### 7. 错的枚举值会安静地返回空

查询传一个不存在的类目（比如模型自己发明一个「伙食费」），不会报错，只会返回
0 笔 —— 然后模型会自信地告诉用户「**你这个月没吃饭**」。

输出看起来完全正常，没有任何东西可以察觉。所以类目参数必须校验，而且错误消息
里要列出合法值（收到它的人下一步动作就是改参数，不告诉他改什么，他只会再猜）。

## 今天踩的坑

### 坑 1：订阅年化差 0.8%（见「今天学到的 5」）

真 bug，测试当场逮住：`assert sub.monthly == money("10.00")` 失败，实际 10.08。

### 坑 2：我在测试里写了段垃圾断言

`test_group_by_category_sorts_by_value_desc` 第一版写成了：

```python
assert [...] == [...][:0] or [...]
```

`[...][:0]` 是空列表，`== empty or [...]` 恒真 —— 这条断言**什么都没测**。
写的时候是拿不准排序结果，就顺手糊了一段「两个都可能」的表达式，而不是去把
答案算清楚。

教训：拿不准期望值时，正确做法是**先跑一遍看真实输出，再判断它合不合理**，
而不是写一条无论如何都能过的断言。

### 坑 3：`yearly_extra` 写了一段又绕又错的代码

```python
span = (self.since - self.since.replace(month=1, day=1)).days or 365
months_left = Decimal(365 - span) / Decimal("30.44")
```

想算「涨价之后一年多花多少」，绕了一大圈算「今年还剩几个月」，而且 `or 365`
那个兜底在 1 月 1 号会给出完全错误的结果。

其实答案就是 `(现价 - 原价) × 12`。**绕的那一圈全是自己想象出来的需求。**
删掉重写，三行。

---

# Day 3 笔记 · Agent 接入 + skill

## 做出来了什么

```
fa/tools/query.py    describe_data + query_transactions
fa/tools/anomaly.py  find_anomalies + list_subscriptions
skills/              monthly-report / reconcile / budget-check（其中两个带 references/）
```

验收：`pytest` 155 passed；端到端问「2026 年 7 月一共花了多少钱」，agent 答
**7,434.96**，和直接调 `query()` 的结果**逐位一致**；问「有没有乱扣钱」时
自动加载 `reconcile` 并读到第三级。

## 今天学到的

### 1. 计划里的验收问题，当天就答不出来

计划写的是「端到端问『上个月外卖花了多少』，数字与直接调 `query()` 一致」。

实际跑第一遍：

```
条件：2026-07-01 ~ 2026-07-31，餐饮外卖，合计
命中 0 笔
结果：0.00
```

因为**分类是第四天的事**。这不是 bug，是我排期时没想清楚：Day 3 的验收问题
依赖 Day 4 的产出。

但真正值得记的不是「排期错了」，而是**这个错会以什么形式暴露出来**：

> 模型拿到 0，会自信地告诉用户「你这个月没点外卖」。

而 `query()` 是逐字正确的、类目名校验也过了、没有任何东西报错。**这是这个项目
从头到尾最怕的那一类失败** —— 错的答案看起来和正确答案一模一样。

### 2. 所以加了两道处理，而不是改验收问题

最省事的做法是把验收问题换成一个不依赖类目的（「一共花了多少」）。但那样只是
把问题从测试里绕开了，**线上仍然会犯**。

加了两道：

**（a）`describe_data` 报分类覆盖率**，让模型知道现在按类目查不到东西。

关键是**算出来，不写死**：

```python
categorized = sum(1 for t in txns if t.category)
if categorized < len(txns):
    lines.append(f"⚠️ 分类情况：{categorized} / {len(txns)} 笔已分类……")
```

写死成「还没分类」的话，第四天分类跑完它就变成一句谎话，而**谎话比没话说更糟
—— 它看起来仍然可信**。这条和 Day 1 那条「提示词不能和事实脱节」是同一个道理。

**（b）`query_transactions` 直接拒绝**按类目查未分类数据，并告诉模型换条路走
（用 `merchant_contains` 按商户名查）。

判据是「**一笔都没**分类」而不是「有部分没分类」—— 部分分类是正常状态，
那种情况下查出来的数字是真实的下界，可以照常报。

结果：实测时模型主动在回答里加了一段「按类目拆不了，因为 0/1061 笔已分类，
那不是没花钱」—— **防呆反过来变成了回答质量的一部分。**

### 3. 三个 skill 的触发场景必须互不重叠

skill 挑不对，通常不是描述写得不好，而是**几个 skill 的适用场景有交集**。

所以写的时候刻意让它们互斥：

| skill | 触发词 |
|---|---|
| `monthly-report` | 「这个月花了多少」「月报」「上个月怎么样」 |
| `reconcile` | 「有没有乱扣钱」「账单正常吗」「帮我查查」 |
| `budget-check` | 「怎么省钱」「哪里花多了」「预算超了吗」 |

实测两个都挑对了。

### 4. 三级披露在真实场景里跑通了

问「帮我看看有没有乱扣钱」，实际发生的调用序列：

```
→ describe_data
→ use_skill({'name': 'reconcile'})                    ← 第二级
→ find_anomalies()
→ list_subscriptions()
→ use_skill({'name': 'reconcile', 'path': 'references/playbook.md'})   ← 第三级
→ query_transactions({'merchant_contains': 'TARGET'})  ← 自己追加的追查
```

三件事值得注意：

- 第三级是**模型自己决定**去读的，不是我写死的流程
- 它按 skill 正文说的「两个工具都要跑」跑了两个 —— **skill 在改变行为，不是摆设**
- 最后它自己追加了一次查询去追查那笔重复扣款，这一步 skill 里没写

如果 skill 只是被读进去、没有改变行为，那它就是一段昂贵的上下文。

### 5. 「数字必须来自工具」这条规则的精确边界

写 skill 的时候撞上一个问题：月报要报环比（「比上月多 1,200」、+17.1%），
但**减法和百分比也是算术** —— 按字面理解，这条规则把月报本身禁掉了。

想清楚之后改成更精确的表述：

> 数字只能来自工具。报差值时必须**把两个原始数字一起写出来**
> （「6 月 7,034.22 → 7 月 8,234.56」），让用户能自己验算。

真正的规则不是「模型不许做任何算术」，而是**答案里不能有用户无法验证的数字**：

- 工具查出来的 → 可验证（用户能自己再查一遍）
- 从两个工具数字相减来的 → 可验证（用户能自己减）
- 凭印象说的 → **不可验证**，这才是要禁的

按字面执行一条规则，有时候会把规则本身的**目的**一起干掉。实测模型照做之后，
回答里两个原始数字都写出来了。

## 今天踩的坑

### 坑 1：验收问题依赖了还没做的功能（见「今天学到的 1、2」）

不是代码 bug，是排期 bug。而且它以「安静的错答案」的形式出现，最难发现。

### 坑 2：`sum()` 的起始值我又绕了一圈

```python
total = sum((s.yearly for s in found), start=found[0].yearly * 0)
```

`found[0].yearly * 0` 是为了造一个 `Decimal` 零 —— 因为我当时觉得「从 models
导入 ZERO」麻烦。但 `found` 空的时候这行会 IndexError，而且谁读到这里都得停下来
想一下 `* 0` 是什么意思。

直接 `from fa.models import ZERO` 然后 `sum(..., ZERO)`。**绕路的代码永远比
直接的代码贵**，哪怕它少写一行 import。

### 坑 3：工具里 import 了一堆用不上的东西

`tools/query.py` 第一版从 `fa.anomalies` 导入了四个检测器、还导入了
`parse_amount` 和 `AGG_LABELS` —— 全是从上一个文件复制粘贴头部的残留。

它们不影响运行（测试全绿），但**会让下一个人以为这个模块和异常检测有关系**。
清掉了。这类残留靠人眼查不住，写了个小脚本扫「import 了但正文里没出现的名字」。

---

# Day 4 笔记 · 分类三层 + Memory

## 做出来了什么

```
fa/normalize.py      商户串归一化（三层的地基）
fa/categorize.py     三层：规则 → 缓存 → LLM
fa/memory/store.py   存取（一个可读可编辑的 md 文件）
fa/memory/extract.py 写入门槛
fa/tools/categorize.py  correct_category（纠正 → 缓存 + 记忆）
fa/tools/memory.py      remember
data/categorize.py     批量分类 CLI + per-class 报告
```

验收：`pytest` 224 passed；冷启动 97.1%（1 次 LLM 调用），二次跑 100% 缓存命中、
0 次 LLM；用户纠正一次之后 **99.9%**；跨进程重启后记忆还在，而且**改变了行为**
（agent 主动说明「GEICO 是年度固定支出，不算异常」）。

## 今天学到的

### 1. 三层的顺序不是随便排的：规则必须在缓存**前面**

两种排法看起来都对，实际差别只有一句话：**改规则要能立刻生效**。

缓存先查的话，一条写错的规则被缓存固化之后就再也不会被走到 —— 就算把规则改对
也一样。错误被永久掩盖，而且**没有任何症状**：缓存命中了嘛，快得很，谁也不会
去查。

所以：规则每次重算，而且**缓存只写 LLM 的结论，不写规则的结论**。这条不写清楚，
下一次有人为了「提高缓存命中率」把规则结论也塞进缓存，这个坑就回来了。

### 2. 缓存是加速器，也是错误放大器

「省 LLM 调用」这个好处一眼可见，但同一个机制的另一面是：一次错分类被写进
缓存，这个商户的**每一笔未来交易**都跟着错。

今天的数字把这句抽象的话变成了具体的东西：

```
冷启动 36 个商户，只发了 1 次 LLM 调用
97.1% accuracy —— 全部损失来自一个商户：TARGET 判成了「超市」
一次判断错 → 30 笔交易跟着错
```

反过来说也一样成立：用户纠正**一次**，修好 30 笔（97.1% → 99.9%）。

**这就是商户级归类的杠杆。** 判对一次省几百次调用，判错一次错几百笔 ——
所以 `correct_category` 这种「让用户改一次」的机制不是锦上添花，它是这个杠杆的
必要配套。

也正因为错误会被放大，**缓存必须可读可编辑可删**。黑盒缓存出的错没法修。

### 3. 归一化两个方向都会出事，而且代价不对称

`UBER *EATS` 和 `UBER *TRIP` 是两个不同类目（外卖 / 打车）。

如果把 `*` 后面的内容当随机码删掉，两个都变成 `uber` —— 缓存把它们混成一个
类目，然后**永远错下去**。这是「太松」。

反过来 `SPOTIFY P0A1B2C3` 不归一化，缓存就命中不了，每笔都要问 LLM。这是「太紧」。

两种代价不对称：

    漏删的代价 = 多花一次 LLM 调用（可恢复）
    错删的代价 = 永久性的分类错误（不可恢复）

所以规则定成**只删能确定是编号的东西**（含数字的 token、门市号、城市州名），
不确定的一律留着 —— 一律往「宁可漏删」那边倒。

### 4. 隐私边界有个意外的好处

送进 LLM 的只有归一化后的商户串，不带金额、日期、账户。这条是卖点三，本来就该做。

但实测发现它还有个副作用是好的：**模型拿不到金额，就没法用「金额小 = 订阅」
这种启发式** —— 而那正是 `AMZN Mktp` 那个坑想诱导它犯的错。实测 23 个商户里
`amzn mktp us` 被判成「购物」，正确。

**信息少反而更准**，因为去掉的正是那条会误导人的信息。

### 5. 记忆的难点在「什么时候记」

存哪里是 trivial 的（一个 markdown 文件就够了）。难的是写入策略：

- **记太多**：一次性细节堆成流水账，注入 prompt 既占地方又稀释真正重要的几条，
  最后模型学会无视整块记忆
- **记太少**：用户教过的事下次还得再教，功能等于没做

最容易犯的错是记一次性细节。「这次看看 3 月」记下来，下次用户问 4 月时它就是
**错的** —— 而且看起来还挺合理，所以特别难发现。

所以门槛拒绝时会**说明理由**。不说明的话模型会换个说法再试一次，而那一次可能
就绕过了门槛 —— 那比一开始就放行还糟。

### 6. 纠正一次分类 = 最自然的记忆写入入口

这两件事同一天做，不是我硬凑的：用户纠正分类时说的那句话，本身就该被记住。

`correct_category` 因此**同时写两个地方**，这不是重复：

- **缓存** —— 让这个商户从现在起归对类（立刻生效）
- **记忆** —— 让 agent 下次答得出「为什么这么归」

只写缓存，用户下次问「你为什么把 target 算成购物」时 agent 答不上来，只能现编；
只写记忆，得等下一次批处理跑完才生效。

而且记忆里记的是「target 归购物**而不是超市**」—— 保留改之前的值。只记新值的话，
下次看到会以为这本来就是用户的偏好，而不是一次纠正，而这两者的可信度不一样。

## 今天踩的坑

### 坑 1：工具接的是「数据」而不是「取数函数」，纠正之后不生效（真设计问题）

第三天的工具是这么构造的：`build_query_tools(txns)` —— 把账单元组直接传进去。

今天写 `correct_category` 才发现问题：**它改了缓存，但 agent 手里的账单还是旧的**。
用户纠正完再问一遍，答案不变。而且不会有任何报错 —— 只是「改了没反应」，
**比根本不能改更让人困惑**。

改成传 `get_bill`（一个返回账单的函数）：工具每次用的时候现取，纠正时清一下
`lru_cache` 就生效了。

顺带这也正好是第七天接 MCP 的接缝 —— 到时候换掉取数函数就行，工具一行不改。
**一个正确的接缝往往在需要它之前就已经是对的了**。

### 坑 2：`/dev/null` 在 Windows 上写不进去

`CategoryCache.load(Path("/dev/null"))` 当默认值 —— 读的时候没事（`is_file()`
返回 False，得到一个空缓存），但 `save()` 会试图往 `/dev/null` 写，在 Windows 上
直接失败。

改成默认走 `config.CATEGORY_CACHE`。教训是：**POSIX 特有的路径不该出现在默认值
里**，哪怕它「看起来只是读一下」。

### 坑 3：状态名启发式会过度删减，我把它如实写进了测试

`FEDEX OFFICE IN` 会被归一成 `fedex` —— 那个 `IN` 跟印第安纳州没关系，
但它长得和州码一模一样，没有任何句法手段能分开。

我可以加更多启发式去挡它，但每加一条就多一条会误伤别的规则。所以决定**留着这条
规则，把已知的失败写进测试**：

```python
def test_the_state_rule_is_a_heuristic_and_does_overstrip():
    """**这条测试记录的是一个已知的过度删减，不是在庆祝它。**"""
    assert normalize("FEDEX OFFICE IN") == "fedex"
```

为什么留着：银行商户串基本都是「商户名 城市 州」的格式，收益大于代价；而且
代价是**缓存 key 变粗**，不是算错钱 —— 真出问题时用户可以改缓存，错误不扩散。

**一条写着「我知道这里会错」的测试，比一条假装这里不会错的测试有价值。**

---

# Day 5 笔记 · 检索（RAG）+ 财务知识库

## 做出来了什么

```
knowledge/            12 篇中文理财文档（个税、五险一金、房贷、信用卡、保险、
                      征信、应急资金、预算、存款理财、消费贷、年终奖）
fa/retrieval/
  chunk.py            按标题层级切 + breadcrumb
  index.py            字符 bigram 分词 + 停用词 + 两路 BM25 + 指纹过期检测
  hybrid.py           RRF 融合
fa/tools/knowledge.py search_knowledge
eval/retrieval.py     命中率评测 + 朴素 grep 对比
```

验收：`pytest` 256 passed；字面问法 15 题**文件@1 14/15、小节@3 14/15**
（朴素 grep 文件@1 11/15）；端到端问「房贷利息能抵多少个税」，
模型自动走 `search_knowledge` 并引用来源。

## 今天学到的

### 1. breadcrumb 是切块存在的全部理由

用户问「房贷利息能抵多少」，`专项附加扣除.md` 里那段的正文写的是
「按每月 1000 元标准定额扣除」—— **一个字都没提「房贷」，也没提「个税」**。

救它的是 breadcrumb：`专项附加扣除.md > 专项附加扣除 > 住房贷款利息`。
把标题层级拼进被检索的文本，这一个块才变得可检索。

这和代码检索里「切块要带上文件路径 + 类名 + 函数签名」是同一条原则：
**块本身往往不自足，得把它的位置一起存下来。**

所以 `Chunk` 把 `breadcrumb` 和 `text` **分开存**，而且是**两路分别检索** ——
标题命中和正文命中是两种不同强度的证据，混在一起就分不出来了。

### 2. 中文的「什么是词」问题，答案是 bigram 而不是分词器

BM25 整套建立在「文档由词组成」上，中文直接把这个前提抽掉了。三个选项：

| 方案 | 问题 |
|---|---|
| 单字 | 分不出「税费」和「费用」—— 共用「费」字就互相召回 |
| 分词器（jieba 等） | 引依赖、要维护词典，而且**分词错误会直接引入检索错误**：把「个人所得」切成「个人/所得」，这个文档就再也搜不到了 |
| **字符 bigram** | 机械的，不会切错；精度比单字好得多 |

选 bigram。它唯一的代价是索引大一些，而这是可以接受的。

### 3. 停用词表非有不可 —— 而且这是今天最大的发现

第一版没有停用词，跑出来一个**看起来像融合层坏了**的结果：

```
问：LPR 是什么
   → [征信.md] 个人征信 > 报告里有什么     ← 完全无关
```

但 `lpr` 明明在 `房贷.md` 里原样出现过。查了索引：**token 在，df=1，freq=6，
正是那个块。** 所以不是索引问题。于是把分数打出来：

```
'lpr'   df=1   idf=3.71   ← 真正的信号
'什么'   df=5   idf=2.41   ← 疑问词
'是'     df=51  idf=0.17

含 LPR 的块：7.37 分
赢它的块   ：9.75 分   ← 靠「什么」「什」「么」赢的
```

**根因是 IDF 在小语料上失效。** IDF 要在大语料上才分得开「稀有因为有意义」和
「稀有因为它是个虚词、恰好只出现在 5 个块里」。这份语料只有 60 个块，一个疑问词
算出来的 idf 只比真正稀有的术语低一点点。

加上停用词表之后，「LPR 是什么」**只返回那一个块**。

**两条教训：**

1. **失败的位置和原因常常不在你以为的那一层。** 我一开始怀疑 RRF 融合，
   实际上融合是好的，问题在一个更基础的预处理步骤上。
2. **不要靠眼看定位问题，把中间量打出来。** 「token 在不在索引里」「每个
   token 的 df 和 idf 是多少」「每个块得了多少分」—— 这三行打印省掉了我
   半小时的瞎猜。

顺带一个设计细节：**由两个停用字组成的 bigram 也是停用词**。不补这条的话，
「的了的」滤完还剩 `的了`、`了的`，于是「查询被滤空了」这个判断永远不成立，
那条退路（滤空了就退回不过滤）就形同虚设。

### 4. 索引指纹里必须带分词方案的版本号

索引带指纹（语料变了就重建），这本来是为了防「改了文档却搜不到」。

但只记语料文件是不够的：**改了分词逻辑，语料一个字节没动，指纹也不变** ——
索引永远不重建，检索会继续用旧方案的排名。测试全绿，线上悄悄不对。

所以指纹 = 语料文件指纹 + `SCHEME` 常量。改分词就必须改 SCHEME。
这是一条**靠纪律维持**的约定，没有东西能自动检查它 —— 所以写在了常量旁边的
注释里，还配了一条测试钉住「SCHEME 变了指纹必须变」。

### 5. 转述组是结构性短板，不是 bug

问「怎么少交点税」，文档里写的是「专项附加扣除」——**字面上一个词都对不上**。

实测：转述组 5 题，本系统 1/5，朴素 grep 2/5。

这个数字不好看，但它是**纯词法检索的定义**，不是实现缺陷。第八天的 RAGAS
`context_recall` 主要就是被这一组拉下来的 —— 有了今天这个前哨数字，
到时候看到低分就知道该往哪儿查，而不是怀疑代码。

也没打算现在加同义词表：**先有数字再决定要不要加**，而不是先加上再说。
（而且 5 道题上 1 vs 2 的差别没有统计意义，拿它当加功能的依据太薄。）

## 今天踩的坑

### 坑 1：指纹用秒级的 `st_mtime`，同一秒内的等长修改察觉不到

`int(stat.st_mtime)` 只有秒级精度。同一秒内改一个**长度没变**的文件
（改个错别字、换个词），指纹纹丝不动，索引不重建。

改成 `st_mtime_ns`。测试里专门钉了一条「长度不变的修改」。

### 坑 2：单个段落本身超长时，`_split_long` 切不动

`_split_long` 是按空行（自然段）切的。如果**一个自然段本身就超过上限**
（比如一段超长的列表），它会把整段原样留下 —— 于是那个块以超限状态进了索引，
而分块上限存在的全部意义就是不出现这种块。

补了一层 `_hard_split`：段落还是超长就硬切。硬切会切断句子，但**被切断的块
仍然能被检索到，一个超大的块则会把检索精度整体拉垮**。

### 坑 3：我的测试正文比 30 字的阈值还短

`MIN_CHUNK_CHARS = 30`，而我写的 fixture 正文是「这里的内容写得足够长，所以这一
节能留下。」—— 20 个字。于是三个块全被当成「只有标题没有内容」的占位块丢掉，
断言 `len(chunks) == 3` 报 `0 == 3`。

我盯着这个断言看了半天，第一反应是切块逻辑坏了 —— 实际上它一直是对的，
是我的测试数据不合格。

**教训：`0 == 3` 这种断言失败，先怀疑输入的规模，再怀疑逻辑。** 后来把
fixture 正文抽成一个显式的 `LONG` 常量，注释里写清为什么要那么长。

### 坑 4：用 heredoc 批量改中文文件，编码会被 shell 搞坏

想用 python 脚本一次替换十几处测试 fixture，结果 heredoc 里的中文经过 Git Bash
之后和文件里的字节对不上，**大部分替换静默失败**（打印出来的「没匹配上的」
列表本身也是乱码）。

改用 Edit 工具逐条改。教训：**批量改含中文的文件不要走 shell heredoc**，
编码这一层不透明，而失败是静默的。

---

# Day 6 笔记 · Agent 工作流 + 子 agent

## 做出来了什么

```
fa/workflow.py          子 agent 机制（Delegation + run_subagent + 工具白名单）
fa/tools/delegate.py    investigate 工具
skills/spending-review  三步工作流（摸清结构 → 逐类目深挖 → 综合）
eval/delegation.py      量上下文省了多少
```

验收：`pytest` 269 passed；多步任务跑通，实测省下 **20,678 字符**。

## 今天学到的

### 1. 隔离才是价值，并行不是

这两件事经常被混着说，但收益完全不同：

- **并行**省的是**时间**。一次子任务也就十几秒，省不出多少
- **隔离**省的是**上下文**，而且**每一轮都在省** —— 主 agent 后面还要接着对话

实测：

```
主上下文总计               14,029 字符
子助手内部过程合计         27,247 字符   ← 没有进主上下文
回传给主 agent 的           6,569 字符
省下                       20,678 字符
```

不隔离的话主上下文会是约 34,700 字符，现在 14,029 —— **降到 40%**。

所以判断要不要开子 agent，标准不是「能不能并行」，而是
**「这个子任务的中间过程有没有价值」**。

这条还有个反面：中间结果**本身就是答案**的时候（「把这 20 笔列出来」），
开子 agent 是有害的 —— 结论就是中间结果，隔离之后信息反而丢了。
所以 `investigate` 的 docstring 里**两个方向都写了**。只写「什么时候该用」，
模型会把它当万能钥匙，每个问题都先派一个。

### 2. 什么写死在代码里，什么交给模型

这是这一天真正要回答的问题。我的分界线是：**能力边界写死，使用决策交给模型。**

**写死在代码里**（这些不是判断题，是安全边界）：

- 子 agent 的工具集**只读**
- **白名单**而不是黑名单
- 不给它 `investigate` 自己 —— 递归的子 agent 是失控的乘数，收益却接近于零
- 步数上限 10（主 agent 是 24）

**交给模型判断**：什么时候该开、子任务怎么切。

后者才真正需要判断力，而且模型手上有足够的信息。前者是硬约束 ——
让模型去「判断」一个安全边界，只会增加不确定性，不会增加智能。

### 3. 白名单必须是 fail-closed 的

用**白名单**而不是黑名单，而且是认真的：**新加的工具默认不给子 agent**。

黑名单的话，以后加一个 `delete_everything` 而忘了往黑名单里加，
子 agent 就悄悄拿到它了 —— 而且**没有任何测试会红**。

白名单的默认方向是相反：忘了加，子 agent 拿不到，功能少一点但不越权。
两种「忘」的代价不对称，所以要往安全那边倒。

配了一条测试钉住这个方向：断言 `SUBAGENT_TOOLS` 是全部工具的**真子集**
（不是相等）—— 相等的话说明有人图省事写了 `= 全部`。

### 4. 记账让「值不值」从说法变成数字

`Delegation` 把这次委派的账记下来：子 agent 用了几步、多少字符，回传多少，
省了多少。这行账**跟着结论一起返回给主 agent**。

不记账的话，「子 agent 到底省没省」永远是个说法。而且**账有可能是负的** ——
中间过程少而结论长时就会是负的，那种情况你会想早点知道。

所以 `report()` 在亏的时候会明说「**反而多了 N 字符（这次不该开子 agent）**」。
一个永远只报好话的记账等于没记账。

### 5. 复用 Session，不另写一个循环

子 agent 和主 agent 走的是**同一个 `Session` 循环**，只是工具集更小、
提示词不同、步数上限更低。

另写一个循环看起来更「干净」（子 agent 不需要事件回调、不需要 skill），
但那意味着**那条「每个 tool_call 恰好一条 ToolMessage」的不变量会有两份实现** ——
而它已经在这个项目里被证明是容易写错的东西（Day 1 花了一整天才钉住）。
两处实现就会漂移，然后有一处忘了改。

代价是 `Session.__init__` 多了三个参数（`tools` / `system_prompt` / `max_steps`）。
这个代价比复制那条不变量便宜得多。

### 6. 工作流写成 skill，而不是写成代码

三步流程（摸清结构 → 逐类目深挖 → 综合）写进了
`skills/spending-review/SKILL.md`，**没有**写成一个 `run_review()` 函数。

理由是这三步里真正需要判断的地方（挑哪几个类目、子任务怎么描述、
结论怎么综合）恰恰是最难写成代码的部分，而写死的部分（调哪个工具）
模型本来就知道。写成 skill 之后，模型自己决定要不要按这个流程走 ——
用户只问「上个月花了多少」时它不会启动整套复盘。

第一步明确写了「**不要派子助手**」：那一步的结果主 agent 自己已经看到了，
派出去等于把它查的东西又隔离掉，还得再问一遍。实测模型照做了 ——
它先 `query_transactions(group_by=category)`，然后才派 4 个子助手。

## 今天踩的坑

### 坑 1：循环 import

```
fa.workflow ──import──▶ fa.tools
fa.tools.delegate ──import──▶ fa.workflow
```

`workflow` 要 import `tools`（拿工具白名单），`tools` 要 import `delegate`
（挂 investigate），`delegate` 又要用 `workflow` 的 `run_subagent`。

修法是把**最里面那个环**在调用时解开：`delegate.py` 在函数体内
`from fa.workflow import run_subagent`。只在需要的时候才解析，import 期不会转圈。

顺带这也影响测试的猴补丁打在哪：因为 import 发生在函数体里，所以每次调用都去读
`fa.workflow` 的**当前**属性 —— 打 `fa.tools.delegate.run_subagent` 是打不到的
（那个名字在模块上根本不存在）。**改 import 的位置会悄悄改变「补丁该打在哪」**，
这条值得记。

### 坑 2：测试里存了 list 的引用而不是快照

假模型里我写的是 `self.seen.append(messages)`，然后断言
`len(model.seen[0]) == 2`。实际是 3。

原因：`messages` 是 session 内部那个**活列表的引用**。模型返回之后 session 又
往同一个列表里追加了 AIMessage，所以事后再看，看到的不是「调用当时的样子」，
而是「跑完之后的样子」。

改成 `self.seen.append(list(messages))`。

**教训：记录「某个时刻的状态」时，永远要拷一份。** 存引用在单线程里都不可靠，
因为持有方还会继续改它。

---

# Day 7 笔记 · MCP：自己写一个 server

## 做出来了什么

```
mcp_server/ledger_server.py   账单数据源 server（JSON-RPC over stdio）
fa/mcp/client.py              手写客户端（不装 SDK）
fa/mcp/adapt.py               MCP tool schema → 内部工具
main.py 的 /mcp 命令          现场看一遍握手
```

验收：`pytest` 295 passed；`/mcp` 能看到完整握手；agent 用
`ledger__fetch_transactions` 列出原始明细（内置工具做不到），并顺手发现了
Day 1 埋的那笔重复扣款。

## 今天学到的

### 1. 独立进程让「边界」从约定变成事实

在同一个进程里，「不把金额发给 LLM」只是一句写在自己代码里的约定 ——
任何一处忘了检查就漏了，而且**没有症状**。

跨进程之后情况不一样：**server 不返回的东西，agent 拿不到**，不是「不该拿」。
约定会被人忘记，进程边界不会。

`list_merchants` 就是这条边界的执行点。归类只需要商户串，所以它就只给商户串 ——
金额、日期、账户一个都不给。测试直接钉住：

```python
assert not re.search(r"\d+\.\d{2}", out)               # 没有金额
assert not re.search(r"\d{4}-\d{2}-\d{2}", out)        # 没有日期
assert "credit" not in out and "checking" not in out   # 没有账户
```

这条测试和别的测试不一样：别的测「代码写对了没有」，这条测「**能力被限制住了
没有**」。后者更容易在后续改动里悄悄失效 —— 有人给 `list_merchants` 加个
「顺便带上金额方便排序」，边界就没了，而所有功能测试照样全绿。

### 2. 运行时发现才是协议的价值，不是「少写几行代码」

三步：`initialize` 能力协商 → `tools/list` 发现 → `tools/call` 调用。

这三步的意义在于**客户端不需要预先知道服务端有什么**。工具是运行时发现的，
schema 是运行时读到的。所以换一个 server、加一个工具，客户端一行都不用改。

plugin 模式要把对方的接口**编进自己的代码**里，所以每加一个就要改一次自己的
代码 —— 而且改完还得重新测试、重新发布。MCP 把这件事从「编译期」挪到了
「运行期」，这才是它值得存在的原因。

### 3. 手写一遍，才知道协议长什么样

装 SDK 当然更快。但那样这个项目就变成「我调用了某个库」。

协议本身小得意外：一行一条 JSON 出去，一行一条 JSON 回来。手写一遍之后，
「为什么 MCP 比每家自己写 plugin 好」这个问题才有具体的答案 —— 而不是
一句从文档里抄来的话。

（顺带：手写才发现有两个传输层的坑，见下。）

### 4. 两个传输层的坑，都是「看起来卡住了」

**stdout 是协议通道。** server 里一个多余的 `print` 就会把协议搅坏 —— 混进去的
那行会被客户端当成一条 JSON-RPC 消息去解析，解析失败，然后表现为
「server 忽然不响应了」。而真正的原因是一行无害的调试输出。

所以 server 里的 `log()` 强制走 stderr。

**stderr 不能接管道而不读。** 如果客户端 `stderr=PIPE` 却从不去读，服务端写满
管道缓冲区就会阻塞在**写 stderr** 上，而客户端在等它的 **stdout** ——
双方互相等，死锁。

这是子进程通信里最经典的一个坑，表现是「跑着跑着不动了」，极难定位。修法是让
stderr 直接继承父进程，顺带日志也不需要额外处理。

## 今天踩的坑

### 坑 1（最重要）：一个「优化」把正确性换掉了，而且是间歇性地换掉

索引的过期检测第一版是这么写的：

```python
parts.append(f"{path}:{stat.st_size}:{int(stat.st_mtime)}")
```

理由很正当：读内容太贵，stat 很便宜。**这个优化是错的。**

同一秒内改一个**长度不变**的文件（改个错别字、换个词），大小和修改时间都不变
—— 指纹纹丝不动，索引不重建，检索继续用旧内容。

第一次发现时我改成了 `st_mtime_ns`，以为精度问题解决了。**没有。** 文件系统的
时间戳粒度不是纳秒，两次写盘落在同一个 tick 里就会拿到相同的时间。

于是 `test_fingerprint_changes_when_the_corpus_changes` **间歇性失败** ——
跑了五六次才露出来一次。间歇性的失败比稳定失败危险得多：它会让人得出
「这个测试有点飘，重跑一下就好」的结论，然后它就再也没被修过。

最后的修法是**老老实实对内容取哈希**。

三条教训：

1. **这个检查存在的全部意义就是「不漏掉变化」。** 一个会漏的检查比没有检查
   更糟 —— 你会以为它兜住了。
2. **要优化就优化调用方，别把正确性换掉。** 真想省，该省的是「每次查询都重算
   指纹」，而不是「指纹算得准不准」。
3. **间歇性失败的测试必须当成真失败查。** 「重跑一次就过了」是掩盖问题的
   开始，不是解决问题的结束。

### 坑 2：合法 JSON 不一定是合法的 JSON-RPC 消息

测试用 `cmd /c echo` 造「服务端发垃圾」，结果 shell 给参数加了引号，
服务端输出的是 `"not json"` —— **带引号的字符串，是合法 JSON**。

于是 `json.loads` 成功返回一个 `str`，然后 `message.get("id")` 抛
`AttributeError`。

这个坑是测试**意外**踩出来的，但它暴露的是真问题：客户端只挡了「解析失败」，
没挡「解析成功但不是对象」。而 `"hello"`、`42`、`[1,2]` 都能过 `json.loads`。
现在会给出干净的 `MCPError`，报错信息也直指原因（服务端往协议通道里写了
非协议内容）。

### 坑 3：`cmd /c` 是 Windows 专有的

上面那条测试我写的是 `StdioClient(["cmd", "/c", "echo", "not json"])`。

**CI 跑在 Ubuntu 上，`cmd` 不存在。** 这是 Day 4 踩过的同一个坑（`C:secret.txt`
那条），这次在推之前自己看见了。

改成 `[sys.executable, "-c", "..."]` —— 跨平台，而且不依赖任何外部命令。

**教训：测试里出现的每一个可执行文件名，都值得问一句「CI 上有吗」。**

## 一处刻意的偏离计划

计划里 server 要暴露 `list_accounts` / `fetch_transactions` / `import_csv`。
**`import_csv` 没做**，是有意的：

那意味着一个**接受文件路径参数**的工具，而这个项目从第一天起的设计就是
**模型永远不提供路径**（当时丢掉 `workspace.py` 就是因为「没有入口，就不需要
守门」）。加一个收路径的工具，等于把那个入口重新打开，而守门的代码早就删了。

数据从哪来是**部署时**的事，不是**运行时由模型决定**的事。想换数据源就换
`data/transactions.csv`，或者给 server 传 `--data` 参数。

## 还没做的

客户端**没有请求超时**。服务端半路死掉（stdout 到 EOF）会报出来，但如果它
活着却不回话，客户端会一直等。

加超时需要一条读线程或者平台相关的多路复用（Windows 上 `selectors` 不支持
管道），而它防的是一个我们自己写的、不会挂起的 server。**这个取舍写在这里，
而不是假装它不存在。**

---

# Day 8 笔记 · RAGAS 评测

## 做出来了什么

```
eval/qa_cases.py    21 道题 + **人工写的**参考答案
eval/collect.py     跑 agent，把每次检索到的块记下来
eval/ragas_run.py   跑 RAGAS 三个指标，写 findings.md
findings.md         结果 + 根因分析（由脚本生成，不是手写）
```

验收：`pytest` 306 passed；三个指标出了真实分数，而且**能看出失败出在哪一端**。

## 结果

| 组 | faithfulness（生成端） | context_precision（检索端） | context_recall（检索端） |
|---|---|---|---|
| 字面问法（17 题） | 0.75 | 0.75 | 0.88 |
| 换种说法（4 题） | 0.02 | 0.25 | 0.25 |

## 今天学到的

### 1. 三个指标的分工，是这天最有用的东西

| 指标 | 测哪一端 | 低了说明 |
|---|---|---|
| `faithfulness` | **生成端** | 模型在编 |
| `context_precision` | 检索端 | 排序不好，前面塞了没用的 |
| `context_recall` | 检索端 | 漏了该有的块 |

一个笼统的「RAG 效果不好」没法指导下一步。「recall 0.88 而 faithfulness 0.75」
立刻告诉你两件事：检索基本对，生成也基本忠实，可以收工了。

### 2. 最大的发现：**转述组的失分不在检索器，在 agent 不去检索**

这一条推翻了我在 Day 5 写下的判断。

Day 5 拿**原始问题**直接查检索器，转述组 1/5，我当时的结论是「纯词法检索的
结构性短板」。今天跑完整链路之后，本地统计出：

```
字面问法（17 题）  真的去检索 17/17   检索到正确文档 17/17
换种说法（4 题）   真的去检索  1/4   检索到正确文档  1/4
```

**检索器在 agent 手里的命中率是 18/18。** 转述组失分是因为 agent **压根没调用
`search_knowledge`** —— 四道题里三道直接从自己的知识里答了，还有一道（「突然
失业了没有收入怎么办？」）跑去**查了账单**。

而 RAGAS 的 `context_recall` 给转述组的分是 **0.25**，正好是 1/4 —— 和「四道题
只有一道去检索了」精确对上。这个吻合本身就在证实诊断。

**为什么和 Day 5 不一样**：agent 会把问题**改写成关键词**（prompt 里要求的那条
「用关键词比用整句准」），而改写恰好补上了没有 embedding 的那个缺口。所以
「原始问题直接检索」的短板，在真实链路里被绕过去了。

**这个诊断决定下一步做什么。** 如果只看三个指标一起塌，最自然的反应是
「检索器不行」，然后去调排序、换分词、上向量 —— 但如果 agent 根本没调用检索，
这些改动一个都不会让分数上升，而你会以为是自己没调对。

所以 `findings.md` 里那节根因分析是**算出来的**，不是写死的。

### 3. 一个错误的指标比没有指标更糟（今天最重要的一课）

第一版 RAGAS 跑出来是：

```
faithfulness 0.10    context_precision 0.06    context_recall 0.00
```

**这是一组完全错误、但长得非常像真结果的数字。** 没有报错、进度条正常走完、
分数落在合理的区间里。

我起疑是因为**两个数对不上**：本地算出的「检索到正确文档」是 17/17，而
`context_recall` 是 0.00。字面组 17 题全部命中，召回率不可能是零。

查下去发现是我的 bug：采集脚本的 `Recorder` 只记了 `source` 和
`breadcrumb`，**没记块的正文**。所以喂给 RAGAS 的 `retrieved_contexts` 是一堆
光秃秃的小节名，里面没有答案需要的任何事实 —— 三个指标自然全部接近 0。

修好之后块的平均长度从 **35 字符变成 203.8 字符**，分数也回到了合理区间。

三条教训：

1. **静默的错误指标会让你去优化错的地方。** 它不只是「没用」，是**有害**的。
2. **抓住它的唯一办法是拿另一个独立算出来的数去对。** 如果我只跑了 RAGAS，
   这组假数字今天就上线了。
3. **把那个直觉固化成检查。** 现在 `sanity_problem()` 会在跑之前看一眼上下文
   的平均长度，太短就拒绝跑并说明原因。

### 4. 防呆自己也会有 bug

上面那个 `sanity_problem` 的第一版**也是错的**：它算的是「每题所有块的总长」
而不是「单个块的长度」。于是 4 个 40 字的标题加起来 160 字，轻松越过 80 的阈值
—— 一个抓不住它要抓的东西的检查。

**分母错了，阈值就没意义。** 修完之后它才真的能在旧数据上报错。

这件事的教训是：写检查的时候要**拿真实的坏数据验一遍它会不会红**。凭直觉设定
的阈值，很可能刚好在错误的那一侧。

### 5. judge 遇到空回复时，那不是一个低分

DeepSeek 的内容过滤是概率性的（见 `deepseek-api-conventions` 那条记忆）。
judge 撞上它的时候会拿到空回复 —— 而空回复会被**当成一次低分**记进指标里，
分数会莫名地飘，而且飘的方向固定向下。

所以 judge 用的模型带重试（`RetryingChatModel`），重试次数和最终仍失败的样本数
都会报出来。

**诚实说明：这轮 0 次空回复、0 次重试。** 也就是说这段代码今天没派上用场 ——
但它防的是一个已经在这个项目里发生过好几次的现象，留着是应该的。写防守代码
时不必因为它「今天没触发」而觉得浪费。

### 6. 一个把 ragas 装不上的依赖冲突

`pip install ragas` 装完 import 不了：

```
ModuleNotFoundError: No module named 'langchain_community.chat_models.vertexai'
```

ragas 0.4.3 在模块顶层 import 了 `ChatVertexAI`，而它声明依赖时
**`langchain-community` 没有版本上限** —— pip 装了最新的 0.4.2，那一版已经把
vertexai 拆成独立包了。

也就是说：**ragas 的最新版和 langchain-community 的最新版不兼容**，而两边都没
报错，只是 import 失败。这是打包方的问题，不是我们的。

修法是把 `langchain-community` 降到 0.3.31（`--dry-run` 确认过它不会连带降级
`langchain-core` 和 `langchain-openai`）。降完 306 个测试全绿。

**教训：装一个重依赖之后，立刻跑一遍全量测试和一次真实调用。** `pip` 会顺手
改一堆传递依赖的版本，而它不会告诉你那些改动有没有破坏什么。

## 关于「人工写参考答案」

计划里说这是主要工作量，确实如此。21 条答案是照着 `knowledge/` 里我自己写的
文档逐条核对写下来的。

为什么不能生成：RAGAS 的 `context_recall` 和 `context_precision` 拿 `reference`
当真值。如果参考答案是模型生成的，这个评测就变成了「模型 A 的答案 vs 模型 B 的
检索」—— 分数反映的是两个模型的口味差多少，而不是检索好不好。更糟的是它**自证**：
检索得好、答案也答得好，分数虚高而没人知道。

`tests/test_eval.py` 里有一条测试钉住「每条题都有非空的、够长的参考答案」——
这类东西坏了不会让任何东西报错，只会让分数悄悄变得没有意义。







