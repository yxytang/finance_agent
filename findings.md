# RAGAS 评测结果

生成时间：本文件由 `python -m eval.ragas_run` 写出，用时 76 秒。
样本：21 题（17 题字面问法 + 4 题换种说法）。
judge：DeepSeek，遇到 0 次空回复（概率性内容过滤），重试 0 次。

## 怎么读这三个数

- `faithfulness` 测**生成端**：答案有没有超出检索到的内容。低了说明模型在编。
- `context_precision` 测**检索端**：排在前面的块有没有用。低了说明排序不好。
- `context_recall` 测**检索端**：该有的块有没有被检索到。低了说明漏了。

**它们能用来看问题出在哪一端，不适合当成精确的性能指标** —— judge 自己会错、成本高、重跑一次分数会动。所以下面的数字都保留两位，不要在小数点后三位上做文章。

### 全部（21 题）

| 指标 | 分数 |
|---|---|
| faithfulness | **0.60** |
| context_precision | **0.70** |
| context_recall | **0.76** |

### 字面问法（17 题）

| 指标 | 分数 |
|---|---|
| faithfulness | **0.74** |
| context_precision | **0.80** |
| context_recall | **0.88** |

### 换种说法（4 题）

| 指标 | 分数 |
|---|---|
| faithfulness | **0.02** |
| context_precision | **0.25** |
| context_recall | **0.25** |

## 一块都没检索到的题

这几题的 `context_recall` 必然是 0，而且原因**不在检索器** —— 是 agent 根本没去检索。

- 怎么才能少交点税？
- 为什么我每个月扣的税不一样多？
- 借钱消费划算吗？

## 根因：失分是路由问题还是检索问题

本轮 21 题里，**18 题真的调了 `search_knowledge`**，另外 3 题一次都没调。

| 组 | 题数 | 真的去检索 | 检索到期望的文档 |
|---|---|---|---|
| literal | 17 | 17/17 | 17/17 |
| paraphrased | 4 | 1/4 | 1/4 |

两组的**检索率**差了 75% —— 一边几乎每题都查，另一边大半不查。这说明组间的分差主要来自 **agent 决定查不查**，而不是检索器查得好不好。**所以该动的是路由（prompt / 触发条件），不是 BM25 的排序。**

> 这个诊断很重要：三个指标一起塌的时候，最自然的反应是「检索器不行」，
> 然后去调排序、换分词、上向量。但如果 agent 压根没调用检索，
> 上面这些改动一个都不会让分数上升 —— 而你会以为是自己没调对。

## 按文档看 recall

这一栏用来回答「是检索没找对地方，还是找对了但答歪了」。

| 文档 | 题数 | 平均 context_recall |
|---|---|---|
| 专项附加扣除.md | 5 | 0.60 |
| 个税基础.md | 2 | 0.50 |
| 五险一金.md | 2 | 1.00 |
| 保险.md | 1 | 1.00 |
| 信用卡.md | 2 | 1.00 |
| 存款与理财.md | 1 | 1.00 |
| 年终奖计税.md | 1 | 1.00 |
| 应急资金.md | 3 | 0.67 |
| 征信.md | 1 | 1.00 |
| 房贷.md | 2 | 1.00 |
| 消费贷与真实利率.md | 1 | 0.00 |

## 已知的偏差（不许为了好看改掉）

1. **知识库是我自己写的。** 语料和参考答案出自同一个人、同一套口径，所以 `context_recall` 天然偏乐观 —— 真实语料上一定会降。
2. **检索是纯词法的**（DeepSeek 没有 embedding 接口，所以没有向量召回）。这个限制是真的，但**本轮数据没有显示它是失分原因** —— 见上面那节。它在 Day 5 用原始问题直接查检索器时量到过（转述组 1/5）；agent 会把问题改写成关键词，改写恰好补上了这个缺口。
3. **judge 用的是同一个模型。** 被评的答案和打分的是同一个 DeepSeek，可能共享同一种偏好。换成不同厂商的 judge 分数会变。
4. **数字会动。** judge 自己会错、路由也不是确定性的 ——重跑一次分数会变几个百分点。所以看趋势，别看小数位。
