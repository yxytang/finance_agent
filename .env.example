# 复制成 .env 再把 Key 填在等号后面（不要加引号、不要加空格）
#
# 注意：这个文件是被 git 跟踪的模板，内容是**要**提交的 —— 别把真 key 写在这里。
# 真正的 key 写进 .env（已被 .gitignore 挡住）。
DEEPSEEK_API_KEY=

# 可选：换模型或端点
# DEEPSEEK_MODEL=deepseek-chat
# DEEPSEEK_BASE_URL=https://api.deepseek.com


# 第十一天的向量检索。**是可选的** —— 不填就只有 BM25，和以前一样。
#
# 默认走阿里云百炼（DashScope）。留空不是错误：向量检索是加强项，缺了它退回
# 纯 BM25，agent 照常跑（和 DEEPSEEK_API_KEY 不一样，那个缺了整个 agent 不能
# 工作）。key 在 https://bailian.console.aliyun.com 拿。
RAG_API_KEY=

# 可选：换 embedding 的端点或模型
# RAG_EMBED_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# RAG_EMBED_MODEL=text-embedding-v4

# 重排。**和 embedding 不是同一个端点** —— embedding 是 OpenAI 兼容形状，重排
# 是各家自己的形状（百炼的 /services/rerank/... 是原生那套）。
#
# 默认值已经填好了（2026-09-23 实测过形状），不填就能用。想关掉重排就把模型
# 置空：`RAG_RERANK_MODEL=`
# RAG_RERANK_BASE_URL=https://dashscope.aliyuncs.com/api/v1/services/rerank/text-rerank/text-rerank
# RAG_RERANK_MODEL=gte-rerank-v2


# 第十一天的检索缓存（embedding + rerank 两层）。
#
# **不填也有缓存** —— 退回进程内的 dict。它已经能拿到绝大部分收益：缓存的价值
# 大部分在单进程内（反复跑评测、反复问相似的问题）。
#
# 填了才有的是「跨进程共享」：web 服务的几个 worker、或者评测和服务同时跑。
# 键是从内容算出来的，所以**没有「清缓存」这个操作**，改了语料自动就是新的。
#
# 本地没有 Redis 也能试：WSL 里 `sudo apt install redis-server` 然后
# `redis-server --daemonize yes`，Windows 这边连 localhost:6379 就行。
# REDIS_URL=redis://localhost:6379/0
